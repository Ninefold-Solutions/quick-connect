<main class="w-full mx-auto space-y-3 py-4">
  <% frame_id = dom_id(@about, "address_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :address, "Address", class: "text-sm font-normal text-gray-500" %>
          <%= form.text_area :address, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>

  <% frame_id = dom_id(@about, "breif_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :breif, "Brief about #{@contact.decorate.display_name}", class: "text-sm font-normal text-gray-500" %>

          <%= form.text_area :breif, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>

  <% frame_id = dom_id(@about, "met_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :met, "How you met #{@contact.decorate.display_name}", class: "text-sm font-normal text-gray-500" %>

          <%= form.text_area :met, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>

  <% frame_id = dom_id(@about, "habit_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :habit, "Habits And Preferences", class: "text-sm font-normal text-gray-500" %>
          <%= form.text_area :habit, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>

  <% frame_id = dom_id(@about, "work_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :work, "Work Information", class: "text-sm font-normal text-gray-500" %>
          <%= form.text_area :work, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>

  <% frame_id = dom_id(@about, "other_turbo_frame") %>

  <%= turbo_frame_tag frame_id, class: "initial" do %>
    <%= form_with(model: @about, id: dom_id(@about), url: contact_about_path(@contact, @about), data: { turbo_frame: frame_id }) do |form| %>
      <main class="w-full mx-auto space-y-3 py-4">
        <div>
          <%= form.label :other, "Other", class: "text-sm font-normal text-gray-500" %>
          <%= form.text_area :other, class: "form-text-field h-20 mt-1" %>
        </div>

        <div class="flex justify-end space-x-2">
          <%= form.button "Save", class: "btn-primary" %>
          <%= link_to "Cancel", contact_about_index_path(@contact), class: "btn-secondary " %>
        </div>
      </main>
    <% end %>
  <% end %>
</main>
